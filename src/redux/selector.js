// import { createSelector } from "@reduxjs/toolkit";

export const themeSelector = state => state.theme.darkTheme
export const showLoadPageSelector = state => state.loadPage.isShow
export const userLoginSelector = state => state.userLogin.userLogin